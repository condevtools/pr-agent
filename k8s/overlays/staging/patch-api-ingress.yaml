apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mr-agent-api
  namespace: mr-agent
spec:
  tls:
    - hosts:
        - staging.mr-agent.example.com
      secretName: mr-agent-staging-tls
  rules:
    - host: staging.mr-agent.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mr-agent-api
                port:
                  number: 3000
